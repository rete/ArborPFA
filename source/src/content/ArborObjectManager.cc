/*
 *
 * ArborObjectManager.cc source template automatically generated by a class generator
 * Creation date : jeu. f�vr. 13 2014
 *
 * This file is part of ArborPFA libraries.
 * 
 * ArborPFA is free software: you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation, either version 3 of the License, or
 * (at your option) any later version.
 * based upon these libraries are permitted. Any copy of these libraries
 * must include this copyright notice.
 * 
 * ArborPFA is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
 * GNU General Public License for more details.
 * 
 * You should have received a copy of the GNU General Public License
 * along with ArborPFA.  If not, see <http://www.gnu.org/licenses/>.
 * 
 * @author Remi Ete
 * @copyright CNRS , IPNL
 */


#include "arborpfa/content/ArborObjectManager.h"

namespace pandora
{

/*********************************************
 * Manager<ArborObject> template implementation
 *********************************************/

template<>
const std::string Manager<arborpfa::ArborObject>::NULL_LIST_NAME = "NullList";

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
Manager<arborpfa::ArborObject>::Manager() :
    m_currentListName(NULL_LIST_NAME)
{

}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
Manager<arborpfa::ArborObject>::~Manager()
{

}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::GetList(const std::string &listName, const ObjectList *&pObjectList) const
{
    typename NameToListMap::const_iterator iter = m_nameToListMap.find(listName);

    if (m_nameToListMap.end() == iter)
        return STATUS_CODE_NOT_INITIALIZED;

    pObjectList = iter->second;
    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::GetCurrentList(const ObjectList *&pObjectList, std::string &listName) const
{
    listName = m_currentListName;
    return this->GetList(listName, pObjectList);
}

//----------------------------------------------------------------------------------------------------------------------------------

template<>
inline StatusCode Manager<arborpfa::ArborObject>::GetCurrentListName(std::string &listName) const
{
    if (m_currentListName.empty())
        return STATUS_CODE_NOT_INITIALIZED;

    listName = m_currentListName;
    return STATUS_CODE_SUCCESS;
}

//----------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::GetAlgorithmInputList(const Algorithm *const pAlgorithm, const ObjectList *&pObjectList, std::string &listName) const
{
    typename AlgorithmInfoMap::const_iterator iter = m_algorithmInfoMap.find(pAlgorithm);

    if (m_algorithmInfoMap.end() != iter)
    {
        listName = iter->second.m_parentListName;
    }
    else
    {
        listName = m_currentListName;
    }

    return this->GetList(listName, pObjectList);
}

//----------------------------------------------------------------------------------------------------------------------------------

template<>
inline StatusCode Manager<arborpfa::ArborObject>::GetAlgorithmInputListName(const Algorithm *const pAlgorithm, std::string &listName) const
{
    typename AlgorithmInfoMap::const_iterator iter = m_algorithmInfoMap.find(pAlgorithm);

    if (m_algorithmInfoMap.end() == iter)
        return this->GetCurrentListName(listName);

    listName = iter->second.m_parentListName;
    return STATUS_CODE_SUCCESS;
}

//----------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::ResetCurrentListToAlgorithmInputList(const Algorithm *const pAlgorithm)
{
    return this->GetAlgorithmInputListName(pAlgorithm, m_currentListName);
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::ReplaceCurrentAndAlgorithmInputLists(const Algorithm *const pAlgorithm, const std::string &listName)
{
    if (m_nameToListMap.end() == m_nameToListMap.find(listName))
        return STATUS_CODE_NOT_FOUND;

    if (m_savedLists.end() == m_savedLists.find(listName))
        return STATUS_CODE_NOT_ALLOWED;

    if (m_algorithmInfoMap.end() == m_algorithmInfoMap.find(pAlgorithm))
        return STATUS_CODE_FAILURE;

    m_currentListName = listName;

    for (typename AlgorithmInfoMap::iterator iter = m_algorithmInfoMap.begin(), iterEnd = m_algorithmInfoMap.end(); iter != iterEnd; ++iter)
    {
        iter->second.m_parentListName = listName;
    }

    return STATUS_CODE_SUCCESS;
}

//----------------------------------------------------------------------------------------------------------------------------------

template <>
StatusCode Manager<arborpfa::ArborObject>::CreateTemporaryListAndSetCurrent(const Algorithm *const pAlgorithm, std::string &temporaryListName)
{

 typename AlgorithmInfoMap::iterator iter = m_algorithmInfoMap.find(pAlgorithm);

 if (m_algorithmInfoMap.end() == iter)
     return STATUS_CODE_NOT_FOUND;

 temporaryListName = TypeToString(pAlgorithm) + "_" + TypeToString(iter->second.m_numberOfListsCreated++);

 if (!iter->second.m_temporaryListNames.insert(temporaryListName).second)
     return STATUS_CODE_ALREADY_PRESENT;

 m_nameToListMap[temporaryListName] = new ObjectList;
 m_currentListName = temporaryListName;

 return STATUS_CODE_SUCCESS;

}

//----------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::RegisterAlgorithm(const Algorithm *const pAlgorithm)
{
    if (m_algorithmInfoMap.end() != m_algorithmInfoMap.find(pAlgorithm))
        return STATUS_CODE_ALREADY_PRESENT;

    AlgorithmInfo algorithmInfo;
    algorithmInfo.m_parentListName = m_currentListName;
    algorithmInfo.m_numberOfListsCreated = 0;

    if (!m_algorithmInfoMap.insert(typename AlgorithmInfoMap::value_type(pAlgorithm, algorithmInfo)).second)
        return STATUS_CODE_ALREADY_PRESENT;

    return STATUS_CODE_SUCCESS;
}

//----------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::ResetAlgorithmInfo(const Algorithm *const pAlgorithm, bool isAlgorithmFinished)
{
    typename AlgorithmInfoMap::iterator algorithmListIter = m_algorithmInfoMap.find(pAlgorithm);

    if (m_algorithmInfoMap.end() == algorithmListIter)
        return STATUS_CODE_NOT_FOUND;

    for (StringSet::const_iterator listNameIter = algorithmListIter->second.m_temporaryListNames.begin(),
        listNameIterEnd = algorithmListIter->second.m_temporaryListNames.end(); listNameIter != listNameIterEnd; ++listNameIter)
    {
        typename NameToListMap::iterator iter = m_nameToListMap.find(*listNameIter);

        if (m_nameToListMap.end() == iter)
            return STATUS_CODE_FAILURE;

        delete iter->second;
        m_nameToListMap.erase(iter);
    }

    algorithmListIter->second.m_temporaryListNames.clear();
    m_currentListName = algorithmListIter->second.m_parentListName;

    if (isAlgorithmFinished)
        m_algorithmInfoMap.erase(algorithmListIter);

    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::EraseAllContent()
{
    for (typename NameToListMap::iterator iter = m_nameToListMap.begin(); iter != m_nameToListMap.end();)
    {
        delete iter->second;
        m_nameToListMap.erase(iter++);
    }

    m_currentListName = NULL_LIST_NAME;
    m_nameToListMap.clear();
    m_savedLists.clear();

    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::CreateInitialLists()
{
    if (!m_nameToListMap.empty() || !m_savedLists.empty())
        return STATUS_CODE_NOT_ALLOWED;

    m_nameToListMap[NULL_LIST_NAME] = new ObjectList;
    m_savedLists.insert(NULL_LIST_NAME);

    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::ResetForNextEvent()
{
    PANDORA_RETURN_RESULT_IF(STATUS_CODE_SUCCESS, !=, this->EraseAllContent());
    PANDORA_RETURN_RESULT_IF(STATUS_CODE_SUCCESS, !=, this->CreateInitialLists());

    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode Manager<arborpfa::ArborObject>::DropCurrentList()
{
    m_currentListName = NULL_LIST_NAME;
    return STATUS_CODE_SUCCESS;
}



/***********************************************************
 * AlgorithmObjectManager<ArborObject> template implementation
 ***********************************************************/

template<>
AlgorithmObjectManager<arborpfa::ArborObject>::AlgorithmObjectManager() :
    Manager<arborpfa::ArborObject>(),
    m_canMakeNewObjects(false)
{

}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
AlgorithmObjectManager<arborpfa::ArborObject>::~AlgorithmObjectManager()
{

}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::CreateTemporaryListAndSetCurrent(const Algorithm *const pAlgorithm, std::string &temporaryListName)
{
    m_canMakeNewObjects = true;
    return Manager<arborpfa::ArborObject>::CreateTemporaryListAndSetCurrent(pAlgorithm, temporaryListName);
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::MoveObjectsBetweenLists(const std::string &targetListName, const std::string &sourceListName,
    const ObjectList *pObjectSubset)
{
    typename Manager<arborpfa::ArborObject>::NameToListMap::iterator sourceListIter = Manager<arborpfa::ArborObject>::m_nameToListMap.find(sourceListName);

    if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == sourceListIter)
        return STATUS_CODE_NOT_FOUND;

    if (sourceListIter->second->empty())
        return STATUS_CODE_NOT_INITIALIZED;

    typename Manager<arborpfa::ArborObject>::NameToListMap::iterator targetListIter = Manager<arborpfa::ArborObject>::m_nameToListMap.find(targetListName);

    if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == targetListIter)
        return STATUS_CODE_FAILURE;

    if (NULL == pObjectSubset)
    {
        for (typename ObjectList::iterator iter = sourceListIter->second->begin(), iterEnd = sourceListIter->second->end();
            iter != iterEnd; ++iter)
        {
            if (!targetListIter->second->insert(*iter).second)
                return STATUS_CODE_ALREADY_PRESENT;
        }

        sourceListIter->second->clear();
    }
    else
    {
        if ((sourceListIter->second == pObjectSubset) || (targetListIter->second == pObjectSubset))
            return STATUS_CODE_INVALID_PARAMETER;

        for (typename ObjectList::const_iterator iter = pObjectSubset->begin(), iterEnd = pObjectSubset->end(); iter != iterEnd; ++iter)
        {
            typename ObjectList::iterator objectIter = sourceListIter->second->find(*iter);

            if (sourceListIter->second->end() == objectIter)
                return STATUS_CODE_NOT_FOUND;

            if (!targetListIter->second->insert(*objectIter).second)
                return STATUS_CODE_ALREADY_PRESENT;

            sourceListIter->second->erase(objectIter);
        }
    }

    m_canMakeNewObjects = false;
    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::MoveObjectsToTemporaryListAndSetCurrent(const Algorithm *const pAlgorithm, const std::string &originalListName,
    std::string &temporaryListName, const ObjectList &objectsToMove)
{
    if (objectsToMove.empty())
        return STATUS_CODE_NOT_INITIALIZED;

    PANDORA_RETURN_RESULT_IF(STATUS_CODE_SUCCESS, !=, this->CreateTemporaryListAndSetCurrent(pAlgorithm, temporaryListName));
    PANDORA_RETURN_RESULT_IF(STATUS_CODE_SUCCESS, !=, this->MoveObjectsBetweenLists(temporaryListName, originalListName, &objectsToMove));

    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::SaveObjects(const std::string &targetListName, const std::string &sourceListName)
{
    typename Manager<arborpfa::ArborObject>::NameToListMap::iterator targetObjectListIter = Manager<arborpfa::ArborObject>::m_nameToListMap.find(targetListName);

    if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == targetObjectListIter)
    {
        Manager<arborpfa::ArborObject>::m_nameToListMap[targetListName] = new ObjectList;
        Manager<arborpfa::ArborObject>::m_savedLists.insert(targetListName);
    }

    return this->MoveObjectsBetweenLists(targetListName, sourceListName);
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::SaveObjects(const std::string &targetListName, const std::string &sourceListName, const ObjectList &objectsToSave)
{
    if (objectsToSave.empty())
        return STATUS_CODE_NOT_INITIALIZED;

    typename Manager<arborpfa::ArborObject>::NameToListMap::iterator targetObjectListIter = Manager<arborpfa::ArborObject>::m_nameToListMap.find(targetListName);

    if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == targetObjectListIter)
    {
        Manager<arborpfa::ArborObject>::m_nameToListMap[targetListName] = new ObjectList;
        Manager<arborpfa::ArborObject>::m_savedLists.insert(targetListName);
    }

    return this->MoveObjectsBetweenLists(targetListName, sourceListName, &objectsToSave);
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::TemporarilyReplaceCurrentList(const std::string &listName)
{
    if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == Manager<arborpfa::ArborObject>::m_nameToListMap.find(listName))
        return STATUS_CODE_NOT_FOUND;

    m_canMakeNewObjects = false;
    Manager<arborpfa::ArborObject>::m_currentListName = listName;
    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::DeleteObject(arborpfa::ArborObject *pT, const std::string &listName)
{
    typename Manager<arborpfa::ArborObject>::NameToListMap::iterator listIter = Manager<arborpfa::ArborObject>::m_nameToListMap.find(listName);

    if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == listIter)
        return STATUS_CODE_NOT_FOUND;

    typename ObjectList::iterator deletionIter = listIter->second->find(pT);

    if (listIter->second->end() == deletionIter)
        return STATUS_CODE_NOT_FOUND;

    delete pT;
    listIter->second->erase(deletionIter);

    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::DeleteObjects(const ObjectList &objectList, const std::string &listName)
{
    typename Manager<arborpfa::ArborObject>::NameToListMap::iterator listIter = Manager<arborpfa::ArborObject>::m_nameToListMap.find(listName);

    if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == listIter)
        return STATUS_CODE_NOT_FOUND;

    if (listIter->second == &objectList)
        return STATUS_CODE_INVALID_PARAMETER;

    for (typename ObjectList::const_iterator objectIter = objectList.begin(), objectIterEnd = objectList.end(); objectIter != objectIterEnd; ++objectIter)
    {
        typename ObjectList::iterator deletionIter = listIter->second->find(*objectIter);

        if (listIter->second->end() == deletionIter)
            return STATUS_CODE_NOT_FOUND;

        delete *objectIter;
        listIter->second->erase(deletionIter);
    }

    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::DeleteTemporaryObjects(const Algorithm *const pAlgorithm, const std::string &temporaryListName)
{
    if (Manager<arborpfa::ArborObject>::m_savedLists.end() != Manager<arborpfa::ArborObject>::m_savedLists.find(temporaryListName))
        return STATUS_CODE_NOT_ALLOWED;

    typename Manager<arborpfa::ArborObject>::AlgorithmInfoMap::const_iterator algorithmIter = Manager<arborpfa::ArborObject>::m_algorithmInfoMap.find(pAlgorithm);

    if (Manager<arborpfa::ArborObject>::m_algorithmInfoMap.end() == algorithmIter)
        return STATUS_CODE_NOT_FOUND;

    if (algorithmIter->second.m_temporaryListNames.end() == algorithmIter->second.m_temporaryListNames.find(temporaryListName))
        return STATUS_CODE_NOT_ALLOWED;

    typename Manager<arborpfa::ArborObject>::NameToListMap::iterator listIter = Manager<arborpfa::ArborObject>::m_nameToListMap.find(temporaryListName);

    if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == listIter)
        return STATUS_CODE_FAILURE;

    for (typename ObjectList::iterator iter = listIter->second->begin(), iterEnd = listIter->second->end(); iter != iterEnd; ++iter)
        delete *iter;

    listIter->second->clear();
    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::GetResetDeletionObjects(const Algorithm *const pAlgorithm, ObjectList &objectList) const
{
    typename Manager<arborpfa::ArborObject>::AlgorithmInfoMap::const_iterator algorithmIter = Manager<arborpfa::ArborObject>::m_algorithmInfoMap.find(pAlgorithm);

    if (Manager<arborpfa::ArborObject>::m_algorithmInfoMap.end() == algorithmIter)
        return STATUS_CODE_NOT_FOUND;

    for (StringSet::const_iterator listNameIter = algorithmIter->second.m_temporaryListNames.begin(),
        listNameIterEnd = algorithmIter->second.m_temporaryListNames.end(); listNameIter != listNameIterEnd; ++listNameIter)
    {
        typename Manager<arborpfa::ArborObject>::NameToListMap::const_iterator listIter = Manager<arborpfa::ArborObject>::m_nameToListMap.find(*listNameIter);

        if (Manager<arborpfa::ArborObject>::m_nameToListMap.end() == listIter)
            return STATUS_CODE_FAILURE;

        objectList.insert(listIter->second->begin(), listIter->second->end());
    }

    return STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::ResetCurrentListToAlgorithmInputList(const Algorithm *const pAlgorithm)
{
    m_canMakeNewObjects = false;
    return Manager<arborpfa::ArborObject>::ResetCurrentListToAlgorithmInputList(pAlgorithm);
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::ReplaceCurrentAndAlgorithmInputLists(const Algorithm *const pAlgorithm, const std::string &listName)
{
    m_canMakeNewObjects = false;
    return Manager<arborpfa::ArborObject>::ReplaceCurrentAndAlgorithmInputLists(pAlgorithm, listName);
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::ResetAlgorithmInfo(const Algorithm *const pAlgorithm, bool isAlgorithmFinished)
{
    ObjectList objectList;
    PANDORA_RETURN_RESULT_IF(STATUS_CODE_SUCCESS, !=, this->GetResetDeletionObjects(pAlgorithm, objectList));

    for (typename ObjectList::const_iterator iter = objectList.begin(), iterEnd = objectList.end(); iter != iterEnd; ++iter)
        delete *iter;

    m_canMakeNewObjects = false;
    return Manager<arborpfa::ArborObject>::ResetAlgorithmInfo(pAlgorithm, isAlgorithmFinished);
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::EraseAllContent()
{
    for (typename Manager<arborpfa::ArborObject>::NameToListMap::iterator listIter = Manager<arborpfa::ArborObject>::m_nameToListMap.begin(), listIterEnd = Manager<arborpfa::ArborObject>::m_nameToListMap.end();
        listIter != listIterEnd; ++listIter)
    {
        for (typename ObjectList::iterator iter = listIter->second->begin(), iterEnd = listIter->second->end(); iter != iterEnd; ++iter)
            delete (*iter);
    }

    m_canMakeNewObjects = false;
    return Manager<arborpfa::ArborObject>::EraseAllContent();
}

//------------------------------------------------------------------------------------------------------------------------------------------

template<>
StatusCode AlgorithmObjectManager<arborpfa::ArborObject>::DropCurrentList()
{
    m_canMakeNewObjects = false;
    return Manager<arborpfa::ArborObject>::DropCurrentList();
}

}

// template initialization
template class pandora::Manager<arborpfa::ArborObject>;
template class pandora::AlgorithmObjectManager<arborpfa::ArborObject>;

/*********************************
 * ArborObjectManager implementation
 *********************************/

#include "arborpfa/algorithm/ArborAlgorithm.h"

#include "arborpfa/content/SimpleCaloHitObject.h"
#include "arborpfa/content/SmallClusterObject.h"

using namespace pandora;

namespace arborpfa
{

ArborObjectManager::ArborObjectManager() :
		AlgorithmObjectManager<ArborObject>()
, m_canCreateConnectors(false)
{

}

ArborObjectManager::~ArborObjectManager() 
{

}


pandora::StatusCode ArborObjectManager::CreateTemporaryListAndSetCurrent(const ArborAlgorithm *pAlgorithm, std::string &temporaryListName)
{
	return AlgorithmObjectManager<arborpfa::ArborObject>::CreateTemporaryListAndSetCurrent(pAlgorithm, temporaryListName);
}


pandora::StatusCode	ArborObjectManager::ReplaceCurrentAndAlgorithmInputLists(const ArborAlgorithm *algorithm, const std::string &newListName)
{
	return AlgorithmObjectManager<arborpfa::ArborObject>::ReplaceCurrentAndAlgorithmInputLists(algorithm, newListName);
}



pandora::StatusCode ArborObjectManager::CreateArborObject(ArborObject *&pArborObject, ArborObject::Type objectType, void *pInputObject)
{
	pArborObject = NULL;

 try
 {
		if (!m_canMakeNewObjects)
			throw StatusCodeException(STATUS_CODE_NOT_ALLOWED);

		NameToListMap::iterator iter = m_nameToListMap.find(m_currentListName);

		if (m_nameToListMap.end() == iter)
			throw StatusCodeException(STATUS_CODE_NOT_INITIALIZED);

		// factory role
		if(ArborObject::SIMPLE_CALO_HIT == objectType)
			pArborObject = new SimpleCaloHitObject((CaloHit *)pInputObject);

		else if(ArborObject::SMALL_CLUSTER == objectType)
			pArborObject = new SmallClusterObject((Cluster *)pInputObject);
//
//		else if(ArborObject::CALO_TRACK_POINT == objectType)
//			pArborObject = new CaloTrackPointObject(pInputObject);
//
//		else if(ArborObject::CALO_TRACK_START_POINT == objectType)
//			pArborObject = new CaloTrackStartPointObject(pInputObject);
//
//		else if(ArborObject::CALO_TRACK_END_POINT == objectType)
//			pArborObject = new CaloTrackStartPointObject(pInputObject);
//
//		else if(ArborObject::TRACK_ENTERING_POINT == objectType)
//			pArborObject = new TrackEnteringPointObject(pInputObject);

		if (NULL == pArborObject)
			throw StatusCodeException(STATUS_CODE_FAILURE);

		if (!iter->second->insert(pArborObject).second)
			throw StatusCodeException(STATUS_CODE_FAILURE);

		return STATUS_CODE_SUCCESS;
 }
 catch (StatusCodeException &statusCodeException)
 {
		std::cout << "Failed to create arbor object: " << statusCodeException.ToString() << std::endl;
		delete pArborObject;
		return statusCodeException.GetStatusCode();
 }

}

} 

